---
phase: 16-package-documentation
plan: 03
type: execute
---

<objective>
Create doc.go files with package overviews and runnable examples for config, router, state, and status packages.

Purpose: Document the supporting infrastructure packages with examples demonstrating configuration loading, workflow routing, state persistence, and status file operations.
Output: doc.go files for internal/config, internal/router, internal/state, and internal/status packages with Example functions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plans in this phase

@.planning/phases/16-package-documentation/16-01-SUMMARY.md
@.planning/phases/16-package-documentation/16-02-SUMMARY.md

# Source files to document

@internal/config/config.go
@internal/router/router.go
@internal/state/state.go
@internal/status/reader.go
@internal/status/writer.go

**Tech stack available:** Go, Viper, yaml.v3, godoc conventions
**Established patterns:**

- Go doc comment conventions (complete sentences, summary first line)
- Square bracket references to related types [Loader]
- Example functions in \_test package
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create internal/config/doc.go with examples</name>
  <files>internal/config/doc.go</files>
  <action>
Create doc.go with:
1. Example_loader function showing config loading from file
2. Example_getPrompt function showing template expansion

Demonstrate Viper-based configuration loading and Go template expansion for prompts.

Use `_test` package suffix for external examples.
</action>
<verify>go build ./internal/config && go test ./internal/config -run Example</verify>
<done>doc.go exists with at least 1 runnable example that passes `go test`</done>
</task>

<task type="auto">
  <name>Task 2: Create internal/router/doc.go with examples</name>
  <files>internal/router/doc.go</files>
  <action>
Create doc.go with:
1. Example_getWorkflow function showing status-to-workflow mapping
2. Example_getLifecycle function showing lifecycle step retrieval

Demonstrate the pure mapping functions for workflow routing.

Use `_test` package suffix for external examples.
</action>
<verify>go build ./internal/router && go test ./internal/router -run Example</verify>
<done>doc.go exists with at least 2 runnable examples that pass `go test`</done>
</task>

<task type="auto">
  <name>Task 3: Create internal/state/doc.go with examples</name>
  <files>internal/state/doc.go</files>
  <action>
Create doc.go with:
1. Example_manager function showing state save/load operations
2. Example_resumeState function showing resume functionality

Demonstrate the Manager interface for state persistence and resume.

Use `_test` package suffix for external examples.
</action>
<verify>go build ./internal/state && go test ./internal/state -run Example</verify>
<done>doc.go exists with at least 1 runnable example that passes `go test`</done>
</task>

<task type="auto">
  <name>Task 4: Create internal/status/doc.go with examples</name>
  <files>internal/status/doc.go</files>
  <action>
Create doc.go with:
1. Example_reader function showing status file reading
2. Example_writer function showing status file updates

Demonstrate the Reader and Writer interfaces for sprint-status.yaml operations.

Use `_test` package suffix for external examples.
</action>
<verify>go build ./internal/status && go test ./internal/status -run Example</verify>
<done>doc.go exists with at least 2 runnable examples that pass `go test`</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./...` succeeds without errors
- [ ] `go test ./internal/config -run Example` passes
- [ ] `go test ./internal/router -run Example` passes
- [ ] `go test ./internal/state -run Example` passes
- [ ] `go test ./internal/status -run Example` passes
- [ ] `just lint` passes
- [ ] Phase 16 complete
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- 4 doc.go files created with runnable examples
- Phase 16 complete with all 9 internal packages having doc.go files
  </success_criteria>

<output>
After completion, create `.planning/phases/16-package-documentation/16-03-SUMMARY.md`
</output>
